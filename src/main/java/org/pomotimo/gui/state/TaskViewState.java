package org.pomotimo.gui.state;

public enum TaskViewState {
    EMPTY,          // if task list should be cleared, e.g. if no presets exist
    TASKLIST        // if task list should be filled with available tasks.
}
